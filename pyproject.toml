[project]
name = "mahjong-switcher"
version = "0.1.0"
description = "Windows desktop account switcher for Riichi City"
readme = "README.md"
requires-python = ">=3.11"

[tool.release]
app_name = "MahjongSwitcher"
entry_point = "launcher.py"
